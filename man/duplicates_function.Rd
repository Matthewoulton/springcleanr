% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/duplicates_function.R
\name{duplicates_function}
\alias{duplicates_function}
\title{Identify and Handle Duplicate Rows}
\usage{
duplicates_function(data, variables = dplyr::everything(), method = "report")
}
\arguments{
\item{data}{A dataframe or tibble to check for duplicates.}

\item{variables}{A character vector of variable names to check for duplicates.
If \code{NULL}, all variables in the dataset are used.}

\item{method}{A string specifying the method to use. Options are:
\itemize{
\item \code{"report"}: (default) Returns a summary count of how many rows occur at each duplication level.
\item \code{"drop"}: Returns the dataset with duplicates removed (keeping the first occurrence) and prints a summary.
\item \code{"select"}: Returns only the duplicated rows (those that appear more than once).
\item \code{"assert"}: Raises an error if duplicates are found, otherwise returns the original data.
}}
}
\value{
A dataframe or tibble, depending on the method:
\describe{
\item{\code{"report"}}{A summary tibble showing duplication levels and how many rows occur at each level.}
\item{\code{"drop"}}{The deduplicated dataset, with a printed summary.}
\item{\code{"select"}}{A tibble of only the rows that are duplicated.}
\item{\code{"assert"}}{The original dataset if no duplicates are found; otherwise, an error is thrown.}
}
}
\description{
This function detects and optionally handles duplicated rows in a dataset,
based on one or more specified variables. You can generate a summary report,
drop duplicates, select duplicates, or assert that none exist.
}
\examples{
df <- dplyr::tibble(
  id = c(1, 1, 2, 3, 3, 3),
  value = c("a", "a", "b", "c", "c", "c")
)

# Report duplicate levels
duplicates_function(df, method = "report")

# Drop duplicates
deduped <- duplicates_function(df, method = "drop")

# Select only duplicates
dupes_only <- duplicates_function(df, method = "select")

# Assert uniqueness
# duplicates_function(df, method = "assert")  # Will raise an error if duplicates are found

}
